// there can be login context as well, product context, card context, there can be multiple context
import React from 'react';

// creating context
const UserContext = React.createContext()



export default UserContext; // exporting context